server:
  port: 8080

spring:
  application:
    name: cinema-booking
  datasource:
    # url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:cinema-booking}
    #   username: ${DB_USER:postgres}
    #   password: ${DB_PASSWORD:123456}
    url: jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres?user=postgres.opbqtnrqrbatctdrqqrg&password=*M-3jjNnV3Y^s5Q
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    user:
      name: si
      password: 1
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 2000ms
      connect-timeout: 2000ms

# Seat hold configuration
seat-hold:
  ttl-minutes: 10
